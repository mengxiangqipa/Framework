package com.library.clippicviewTest;

public class Test {
    private void doing() {
        /*//1.初始化
        ClipImageLayout mClipImageLayout = (ClipImageLayout) findViewById(R.id.id_clipImageLayout);
        mClipImageLayout.setClipLayoutCircle(context);
        mClipImageLayout.setClipZoomImageViewResource(R.drawable.actionbar_clip_icon);
        mClipImageLayout.setClipLayoutWidth(100, context);
        //2.裁剪
        Bitmap bitmap = mClipImageLayout.clip();//裁剪得到目标bitmap
        //3.将裁剪的bitmap转换成byte[] 传输
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        bitmap.compress(Bitmap.CompressFormat.JPEG, 100, baos);
        byte[] datas = baos.toByteArray();
        Intent intent = new Intent(this, xxx.class);
        intent.putExtra("bitmap", datas);
        context.startActivity(intent);
        //4.接收端解析
        byte[] b = getIntent().getByteArrayExtra("bitmap");
        Bitmap bitmap = BitmapFactory.decodeByteArray(b, 0, b.length);*/
    }
}
